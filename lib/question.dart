class Question {
  String questionText;
  bool questionAnswer;

// this is positional parameters: where argument passed in same order as the parameters written
  Question(this.questionText, this.questionAnswer);
}
